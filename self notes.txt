Reference
-Work ONLY in the src trash_sorter

What do
-Create parts, assemble
-Extend arm, robot(?) python files to include motorized bin
-Work on poses and challenge nodes (see separate branches)
    -Can work on shuttle strat while working on motorized bin

Optional
-Use different branches

About bash scripts
-They're dasy-chained scripts that should allow you to easy open the GUI by running the first script (testbash1.sh)
within a new terminal.
